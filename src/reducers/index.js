const initialState = {
  categories: [],
  posts: [],
  comments: []
}

export default function readable(state = initialState, action) {
  switch (action.type) {
    
    default:
      return state
  }
}